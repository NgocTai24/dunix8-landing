# Workflow

1. Them hoac sua file markdown trong `content/products` hoac `content/news`.
2. Kiem tra frontmatter day du theo mau hien co.
3. Chay `npm run lint`.
4. Chay `npm run build` truoc khi deploy.
